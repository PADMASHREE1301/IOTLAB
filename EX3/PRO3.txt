from machine import Pin
import time

# Use GPIO 17 (GP17 pin on Pico)
led = Pin(17, Pin.OUT)

while True:
    led.value(1)   # LED ON
    time.sleep(1)  # Wait 1 second
    led.value(0)   # LED OFF
    time.sleep(1)  # Wait 1 second